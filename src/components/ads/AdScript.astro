---
/**
 * The AdSense Auto ads loader, emitted once per page from <head>.
 *
 * Auto ads need only the publisher ID in the script URL. Placement and formats
 * are configured in the AdSense dashboard, not in code. An unconfigured build
 * ships no request to googlesyndication.com at all.
 */
import { ads } from '@/features/ads/env';
---

{
  /*
   * `is:inline` is required. Without it Astro treats this as a script to
   * process and bundle, and a third-party loader that must run from Google's
   * own origin -- with its own caching and its own `client` query -- is exactly
   * the kind of script that must be left alone.
   */
  ads.enabled && (
    <script
      is:inline
      async
      src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${ads.client}`}
      crossorigin="anonymous"
    />
  )
}
